## Mandatory Skill Loading

- **코드를 쓰거나 고치기 전에 `coding_rules` skill을 반드시 로드하라.** 판단하지 말고 무조건 로드하라.
  Edit/Write로 `.py` 파일을 건드리는 모든 경우가 해당된다.
- **md 문서를 생성하거나 수정하기 전에 `md_rules` skill을 반드시 로드하라.**
  Edit/Write로 `.md` 파일을 건드리는 모든 경우가 해당된다.
